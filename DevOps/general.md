### 代码托管平台(gitlab)
2
 ```
 - git
   -- code version
   -- .gitignore
   -- git config
 -gitlab
   -- .gitlab-ci.yaml
   -- gitlab: merge->comment->e-mail->jira;build images;save images;auto test 
 ```
